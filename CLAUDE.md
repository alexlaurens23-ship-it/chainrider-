# CHAINRIDER

> Permanent session context. **Read this first, every session.** Update the **Status** and **File map** sections at the end of every session.

## Project

**CHAINRIDER** — a 2D physics motocross game where you ride real crypto price charts as terrain. Per-track leaderboards. **30-minute, UTC-aligned SOL payout windows** to top scorers, **paid manually by the owner from an admin panel**. Launches as a **pump.fun token** whose **creator fees fund the prize pool**.

## Stack

Monorepo via **npm workspaces**.

| Path | What | Tech |
|---|---|---|
| `packages/physics` | Deterministic sim **+ all scoring**. Shared by browser **and** Node. | Planck.js (`planck@1.5.0`), TypeScript |
| `apps/web` | Game client (render, input, UI, skins). | Vite + TypeScript + Canvas2D |
| `apps/api` | Leaderboard, run re-sim/validation, payout windows, admin panel. | Fastify + TypeScript, Supabase (Postgres), node-cron |
| `scripts/launch` | Token launch tooling. | PumpPortal |

- **All Postgres tables prefixed `cr_`** (e.g. `cr_tracks`, `cr_runs`, `cr_payout_windows`).
- `packages/physics` is the source of truth both apps import — it must build for both browser and Node targets.

## Hard rules (do not violate)

1. **Tracks are frozen & versioned.** Never mutate an active track's points/data. Changes = a new track version, never an in-place edit.
2. **Scoring lives ONLY in `packages/physics`.** `apps/web` and `apps/api` both import it. **Never duplicate or reimplement scoring logic** anywhere else.
3. **Fixed timestep `1/60`s everywhere.** Rendering interpolates between sim states; the **simulation never reads wall-clock time** (`Date.now()`/`performance.now()` are banned inside the sim). Time = `stepIndex * SIM_DT`.
4. **Server re-simulates every submitted run** (replay its input log in `packages/physics` on Node) before it can rank or be paid. Client scores are never trusted.
5. **No secrets in code.** `.env` only. No keys, mnemonics, RPC URLs, or service tokens committed.
6. **No Tron IP anywhere.** Original neon bike only — no Disney/Tron assets, traced designs, or the words "Tron"/"light cycle" in art, code, filenames, or marketing. (Crypto product → real IP risk; Disney has defeated Tron-brand trademarks before.)
7. **Do not touch any other project on this machine or VM.** Stay inside the CHAINRIDER repo.

## Key constants

| Constant | Value | Notes |
|---|---|---|
| `SIM_DT` | `1/60` s | Fixed timestep, everywhere |
| Gravity | `(0, -10)` m/s² | |
| pump.fun launch market cap | **$4,000 USD** | **Hardcode. Never scrape.** |
| Payout window | **30 min, UTC-aligned** | Windows start on the UTC clock (`:00`/`:30`) |
| Planck version | `planck@1.5.0` (MIT) | Pinned exact; `planck-js` is deprecated |
| Solver iterations | velocity **8**, position **3** | Identical browser + Node |
| Dev ports | web **:5180**, api **:8787** | Vite `strictPort: true` — 5173 belongs to another project on this machine; never fall back |

## File map

> Fill in one-liners as phases complete.

```
CLAUDE.md                     This file — permanent context.
package.json                  Workspaces root. Scripts: dev / build / verify.
tsconfig.base.json            Shared strict compiler options; every workspace extends it.
docs/
  PHYSICS_RESEARCH.md         Physics research + final rig spec (see appendix below).
  ART_RESEARCH.md             Art research + neon bike visual spec (see appendix below).
packages/
  physics/                    Deterministic sim + scoring. tsup dual ESM/CJS (dist/index.js + .cjs).
    src/sim.ts                createSim (terrain+rig+checkpoints) / stepSim (ALL input/control/crash/scoring per tick) / getSnapshot / getTrackInfo.
    src/replay.ts             simulateReplay(track, tune, inputLog, maxTicks) → FinalResult (incl. maxX = furthest forward chassis x reached; P7.4 progress probe — passive, non-behavioral). Server calls this in P6/P7.4.
    src/scoring.ts            TIME-PRIMARY scoring (sole impl): SCORING_CONFIG (baseFinish 10000/speedExp **1.25**/trickWeight 0.35/**trickAmplifyK 25000**/crashTimePenaltyMs 3000/parPaceMps {VOLATILE:8,DEGEN:7,SAVAGE:6}) + computeFinalScore (**P8.14 MULTIPLICATIVE finish: round(speedScore·(1 + rawTrickPoints/trickAmplifyK))** where speedScore=round(baseFinish·(par/effTime)^1.25); DNF=trickBonus only) + updateScore (live HUD score = trick tally during run; multiplicative final on the finished frame). **P8.12→P8.14: speedExp 1.5→1.25; trickWeight 0.15→1.0→0.35 (now only DNF/breakdown); tricks AMPLIFY speed (multiplicative) so a slow/crashy run can't out-farm a clean fast one.** SCORING = trick-detection values (flip 250, airtime 10/6t, wheelie 20/60t, cleanLanding 50, comboMax 5/120t).
    src/sim.ts (control)      stepSim drive block: LOW-SPEED LAUNCH ASSIST (grounded+throttle+preSpeed<launchSpeedThreshold(3) → motorTorque ×launchBoost(1.8) → ×1 above) + CONTEXT-AWARE REVERSE (forwardVel>reverseEngageSpeed(1.5) or airborne → brakes unchanged; else grounded crawl → rear motor reverses at reverseMotorTorque(60)/reverseMotorSpeed(17), capped reverseMaxSpeed(5.5)) + REVERSE INCLINE TRACTION (reversing up a slope < -reverseHillMinSlopeDeg(15°) → chassis surface force ×reverseHillAssist(**0.65**, P8.5)·m·g·sin, grip-independent — climbs out of steep Vs; mirrors hillAssist(**0.6**, P8.5)). General handling otherwise locked.
    src/terrain.ts            Pure polyline geometry: y/slope at x, wrapAngle, swept head-circle death check.
    src/constants.ts          SIM_DT, gravity, iterations, SIM_VERSION=14 (bump on any physics/scoring change; P8.12 trick rebalance), lead-in/run-out/checkpoint/freeze constants.
    src/types.ts              INPUT bitmask, BikeTune + DEFAULT_TUNE (incl. launchSpeedThreshold/launchBoost), Sim, SimSnapshot, TrackInfo, FinalResult (incl. maxX, P7.4).
    test/{grounded,scoring,respawn,launch,reverse}.test.ts  Vitest (15): incline climb + hard-landing + bit-identical replay; anti-exploit; respawn clearance; launch assist; context-aware reverse (forward brake decelerates > coast; standstill S backs up >3m, capped; steep-V escape: reverses up out of a 45° valley, assist > no-assist control).
apps/
  web/                        Vite + vanilla TS. Hash-routed SPA: #/ home, #/map/:slug/:period, #/ride/:trackId.
    vite.config.ts            Dev server pinned to :5180 (strictPort) + /api proxy -> :8787; **P9.0: host:true + allowedHosts [.trycloudflare.com/.ngrok-free.app/localhost] for tunneling**. Frontend API base = `import.meta.env.VITE_API_BASE ?? "/api"` (relative; net.ts) — works behind a tunnel/same-origin. `src/vite-env.d.ts` types VITE_API_BASE (tsconfig types:[]).
    index.html                #app root + full <style> (palette, pages, ride HUD, run-complete card, #wallet-bar + username .modal).
    src/main.ts               mountWalletButton() + mountMusicControls() (persistent, outside #app) + mounts the router into #app.
    src/audio/music.ts        (P8.1) Procedural synthwave engine (Tone.js, NO audio files): 4 generative ~2-min tracks (0 mellow menu / 1 euphoric / 2 dark / 3 fast ride), minor-key pad+bass+16th arp+drums, side-chain pump LFO. Autoplay-safe (starts on first gesture, silent if cr_muted), master 0.35; route-aware (menu vs #/ride|#/replay), auto-advance + skip. initMusic/setMusicMode/skipTrack/toggleMuted/isMuted.
    src/audio/controls.ts     (P8.1) Fixed-corner mute (🔊/🔇, persisted cr_muted) + skip (⏭) buttons; calls initMusic.
    src/auth.ts               Account UI (P6-rev/rev2): persistent #wallet-bar (logged-out: "Log in to ride & win SOL" tagline + Log In / Sign Up; logged-in: @user·Log Out) — HIDDEN on #/ride/* (hashchange listener) so it doesn't overlap the minimap. LOGIN + 2-step SIGN-UP modals (cross-linked, carry an onSuccess continuation). requireLogin(onSuccess)/isLoggedIn() gate riding (browsing is free); expired JWT self-clears on load (isTokenExpired). JWT+username in localStorage; getToken() is the Bearer source for net.ts. No Phantom/wallet-adapter (vanilla TS).
    src/router.ts             Hash router: #hash → screen {mount/unmount}; body.no-scroll for game screens. Routes: #/ home, #/map/:slug/:period[/:tier], #/ride/:trackId, #/replay/:runId, #/payouts, #/admin.
    src/net.ts                apiFetch (Bearer JWT + **x-admin-key from sessionStorage(ADMIN_KEY_STORAGE)**; surfaces server {error}) + fetchers + caches. SubmitRunPayload→SubmitRunResult; postSignup/postLogin. P7: getPayoutBoard/getGlobalLeaderboard(scope)/getMyLeaderboard/getPaidReceipts/getReplay + admin getPendingPayouts/markPaid/skipPayout/getFlaggedRuns/approve|rejectRun/getWindowHistory.
    src/ui/format.ts          difficultyColor + tierColor (VOLATILE amber/DEGEN red/SAVAGE magenta), formatSol/Score/Clock/Countdown.
    src/ui/sparkline.ts       drawSparkline (home cards). ui/chartPreview.ts: drawChartPreview (map detail, green-up/red-down + fill).
    src/shared/bike.ts        (P5.5) drawBike — **3-PIECE HYBRID SPRITE bike**: frame+rider PNG at the chassis pose; front + rear wheel PNGs each at their OWN physics-body position + spin rotation → real suspension flex + spinning wheels; all tumble on crash. Draw order wheels→frame. Tunables in the exported **mutable `BIKE_TUNE` object** (P5.6 — FRAME_WIDTH_M, FRAME_OFFSET_X/Y, SPRITE_ROTATION_OFFSET, FRONT/REAR_WHEEL_DIAMETER_M, FRONT/REAR_WHEEL_OFFSET_X/Y; drawBike reads it each frame so the dev panel can edit live; **frame offset + wheel align-offset both rotate with the chassis angle so pieces stay aligned under flips/wheelies/slopes — each wheel anchors to its OWN physics-body world position + spins by its body angle. P5.7 forkless Tron art: hub-centred identical ring wheels float (no drawn forks), defaults reset to neutral (offsets 0) for re-dialing**). Skin.sprites = {frame, wheelFront, wheelRear} paths (all → green set `/bike-frame.png` + `/bike-wheel-{front,rear}.png` for now). Sprite cache + `preloadBikeSprites()` (skips a piece until decoded). BikeView carries skin + inputMask (inputMask unused). Cosmetic only; glow trail stays code-drawn behind it.
    src/shared/trail.ts       (P5/P5.1) createTrail — rear-wheel light ribbon: ring buffer (52 world pts), tapered additive polyline (**P5.1: thick light-wall head width 11→0, alpha .85→0**) in skin.trail; pulse() flash on flip-land/combo; clear() on respawn. The marketing money-shot.
    src/skins.ts              (P5) Skin {id,name,primary,secondary,trail,unlock} + 4 SKINS (MINT/VOLT/EMBER/MAGMA, all 'default'); getActiveSkin/setActiveSkin (localStorage cr_skin); createSkinPicker (swatch row, reused on run-complete + mapDetail); prefersReducedMotion(). Hex-swap only; token-gated unlocks wired ('later') but none at launch.
    src/screens/home.ts       Hero + stats strip + **trending coin cards FIRST** (P7.1) + **MAIN PAYOUT BOARD under them (10 PAYING raw tracks, prize, current-window leader/"open", apex #1 glow, one countdown to ends_at)** + "see who we've paid".
    src/screens/mapDetail.ts  Period × TIER × RAW/SMOOTH selection → preview/stats/prize + **(P7) GLOBAL board (This Window / All-Time tabs) + YOUR PBs (top-5 + all-time rank when logged in)** — both re-query on selection change — + RIDE target.
    src/screens/admin.ts      (P7/P7.4) #/admin key-gated, mobile-dense: unlock (sessionStorage cr_admin_key, 401→re-lock) → PENDING (window→track groups; **prominent WATCH REPLAY "review before sending SOL" step per row** (P7.4 manual anti-cheat); wallet/amount one-tap copy; MARK PAID(txSig)/SKIP(reason); total) · FLAGGED (claimed score, WATCH REPLAY, approve/reject) · WINDOWS (history).
    src/screens/payouts.ts    (P7) #/payouts public receipts feed: time/track/rider/amount + tx sig → solscan.io/tx/{sig}.
    src/screens/replay.ts     (P7) #/replay/:runId read-only replay — reuses createRideLoop with a log-driven readMask (walks inputLog by tick), createRideRenderer + HUD. No input, no submit, no login gate.
    src/screens/ride.ts       Wires fetch→loop+renderer+hud+input+run-complete; run AUTO-SUBMITS on end (finish/quit) via the card's autoSubmit (no Submit button).
    src/ride/loop.ts          Fixed-timestep ride loop; change-only [tick,keymask] log (P6 replay); maxCombo/air/speed tracking; 20-min (72000-tick) cap; respawn/quit; **mask in RideFrame (rider lean) + P5 crash slow-mo (frameTime ×0.4 for 500ms on crash edge — real-time pacing only, tick stream/log unchanged; off under prefers-reduced-motion)**.
    src/ride/render.ts        Camera (smoothed lookahead + speed zoom 1.0→0.8, kill-floor clamp); terrain culled via binary search (visibleRange) + green/red glow + gradient fill + gridlines; baked minimap + position dot; **P5: reads getActiveSkin() per frame + passes skin/mask to drawBike; owns the rear Trail (push/pulse/draw) + crash spark particles + ~250ms screen shake (shake off under prefers-reduced-motion); reset() clears effects. render() gained a mask param.** **P8.3: neon checkered finish flag (drawFinishFlag) at the track end, labelled with the rank-1 prize (setFinishPrize).** **P8.2: floating neon "+N" flip popup — on `curr.flips>lastFlips` spawns a world-anchored "+<live score delta>" above the bike that rises 40px + fades over 0.8s (reads the real awarded score; no scoring change).**
    src/ride/chart.ts         segmentColor (up=green/down=red) + visibleRange (binary-search cull). ride/hud.ts: DOM HUD. ride/input.ts: keymask+R/M/Esc+dispose. ride/runComplete.ts: star card + speed/trick breakdown + **the displayed score IS official (no "estimate" label — P7.4 trusts the client score)** + auto-submit status ("Connect wallet"; Saving…→"VERIFIED ✓ · #win·#all-time" / "VERIFIED ✓ — reach the flag to rank"(DNF) / "Under review"(flagged) / "Couldn't verify — invalid run"(malformed)) + Retry/New Track.
  api/                        Fastify on :8787 (ESM, tsx dev). Track pipeline + game endpoints live.
    src/trackgen.ts           PURE deterministic generation: normalize/rawTrack/smoothTrack/stats. TIERS=VOLATILE(×1.8,0.4)/DEGEN(×2.8,0.9)/SAVAGE(×3.6,1.2) via amplify+roughness(seeded)+per-segment-55°-clamp. generateTier(closes,tier,periodAmp=1); PERIOD_AMPLITUDE{1Y:1,6M:1.25,3M:1.5}. stats() also returns difficultyScore = steep-segment density (>35°) + avg-slope×1e-6 tiebreak, round8 (the payout-pool grade). Golden-tested.
    src/chartdata.ts          fetchCloses(source, sourceId, period): Period=1Y/6M/3M → CoinGecko days 365/180/90 (daily-bucketed) + GeckoTerminal. Retry ×3 expo backoff. ONLY place external APIs are called.
    src/db.ts                 Lazy service-role Supabase client (getDb).
    src/windows.ts            Window engine (P7, SINGLE-INSTANCE — node-cron is in-process): WINDOW_MS/slotStartMs; ensureOpenWindow (shared bucketing helper, runs.ts imports it); closeElapsedWindows (open windows past ends_at → EXISTING closeWindow(), then **notifyWindowClose if inserted>0** — Telegram, try/caught so it never breaks the close); startWindowEngine(app) (boot catch-up close+open, then cron "0,30 * * * *" UTC). Logs open/close.
    src/payoutOps.ts          (TG bot) Shared Supabase payout ops — SINGLE source of truth for both the admin web route AND the Telegram bot: isValidSolanaSig (bs58→64 bytes) / fetchUnpaidPayouts(db,windowId?) → EnrichedPayout{id,wallet,username,label,amountSol,rank} / markPayoutPaid(db,id,txSig) (validate exists+pending+sig → status='paid'+tx_sig+paid_at; returns paid|not_found|not_pending|bad_sig|error). No keys, no SOL movement.
    src/daily.ts              (P7.6) Daily challenge engine, SINGLE-INSTANCE in-process cron `0 0 * * *` UTC. PURE: dayBoundsUTC(now)→{challengeDate,startsAt,endsAt} / pickRandomTrackId(ids) (Math.random — not the sim) / pickDailyWinner(runs)→top server_score (time tiebreak). I/O: dailyPrizeSol(db) (cr_config.daily_prize_sol, default 0.5) / ensureTodayDaily (pick random active track if no row for today's UTC date; idempotent on challenge_date) / settleDaily (top verified+finished run in [starts_at,ends_at) → winner + insert cr_payouts kind='daily' + notifyDailyWinner; no finisher→settled no payout) / settleElapsedDailies (boot catch-up) / startDailyEngine(app). Started in index.ts.
    src/telegram.ts           (TG bot, Option B notify-and-reply) Single-user payout control channel — NEVER holds keys / sends SOL / auto-pays; obeys ONLY TELEGRAM_ADMIN_USER_ID (others ignored silently); disabled if env unset. Zero-dep (built-in fetch: sendMessage + getUpdates long-poll). PURE: buildPayoutNotification (null⇒silent on empty) / buildPendingList / parsePaidCommands (1+ /paid lines) / re-exports isValidSolanaSig. I/O: notifyWindowClose / handlePaid (→ "✓ Paid…" + solscan link) / handlePending / **(P7.6) buildDailyWinnerNotification + notifyDailyWinner (announces a settled daily's winner; /paid/markPayoutPaid unchanged — daily payouts are ordinary cr_payouts rows)** / startTelegramBot(app) (boot-backlog-skipping poll loop, admin-gated, single-instance; raises net.setDefaultAutoSelectFamilyAttemptTimeout to 5s — undici's 250ms default is shorter than the ~300ms TCP handshake to api.telegram.org, so fetch ETIMEDOUTs while curl works). Started in index.ts after startWindowEngine.
    src/leaderboards.ts       PURE leaderboard shaping (P7, tested): topPerPlayer (one row per player = their best, score-desc/time-asc, top N, ranks) + rankOfPlayer. No client stats ranked.
    src/auth.ts               JWT primitives (P6-rev/P6b): signToken+verifyToken **pinned to HS256** (algorithms:['HS256']) (7d {playerId,username}) / jwtSecret() + assertJwtSecretStrength() refuse a secret <32 chars (called at boot in index.ts) / requireAuth preHandler / validateUsername. Separate from admin X-Admin-Key.
    src/accounts.ts           Account domain (P6-rev/P6b, pure + repo-injected like payouts.ts): validatePin / validateWalletAddress (bs58→32 bytes) / hashPin+verifyPin (bcryptjs cost 10 — PIN ONLY ever a hash) / evaluateLogin (reference lockout state machine, MUST mirror sql/006: 5 wrong → lock, **ESCALATING ×2 per consecutive lockout** 15→30→60min capped 64×; expired lock self-clears; active lock rejects even a correct PIN; resets on success) / performSignup / performLogin (delegates the wrong-PIN increment to the ATOMIC repo.registerLoginFailure — never recomputes from a stale read; generic 401 unknown / 403 banned / 423 locked) / AccountRepo (registerLoginFailure via cr_register_login_failure RPC + recordLoginSuccess; findByUsername/usernameTaken use **.eq lowercased** not .ilike). now injectable for tests.
    src/runVerify.ts          PURE TRUST-CLIENT-SCORE verify (P7.4 — replaces P7.2 authoritative-server; cross-engine float makes the server's replayed score unusable on hard tracks). verifyRun({points,parTimeMs,worldLength,inputLog,submittedTicks,client:{score,flips,finished,timeMs},maxScore}): isWellFormedLog (strictly-increasing [tick,mask]≤90000, last<ticks, ticks≤72000) → else 'failed'; simulateReplay(DEFAULT_TUNE) **capped at submittedTicks** ONLY for its maxX (progress probe, score never used to rank) → spawnX=points[0].x−LEAD_IN/2, progress=maxX−spawnX < **PROGRESS_MIN_METERS(25)** → 'flagged' (no real ride; does NOT require finish); client.score>maxScore (ceiling) → 'flagged'; client.score>maxPlausibleScore (generous band sized by ticks+claimed flips capped by ticks, ×2 safety) → 'flagged'; else 'verified'. The CLIENT's score/flips/finished are official (rank+display); manual replay review (admin WATCH REPLAY) is the payout anti-cheat. maxReasonableScore (GHOST 60m/s, ×2) + maxPlausibleScore are pure/tested.
    src/routes/tracks.ts      mapsRoutes (GET /api/maps: per-map tiers{VOLATILE/DEGEN/SAVAGE}{raw,smooth,prize} + legacy difficulty/tracks from VOLATILE; tier-keyed prizeLadder) + tracksRoutes (GET /api/tracks/:id: frozen points + tier).
    src/routes/admin.ts       X-Admin-Key gated: POST /maps (fetch period closes→generateAllTiers: 3 tiers × 2 modes = 6 frozen tracks, tier amplify ×PERIOD_AMPLITUDE[period], par per tier), POST /maps/:id/regenerate.
    src/routes/stats.ts       GET /api/stats: {ridesCompleted, totalSolPaid, config:{windowMinutes,maxScoreDefault}}. Never 500s (Home must render).
    src/routes/payoutPool.ts  GET /api/payout-pool (P7.1): live top-10 hardest RAW tracks by difficulty_score → {rank, prizeSol, ...}. Rule-based (cr_config.payout_tiers, poolSize 10). Max 0.85 SOL/window.
    src/payouts.ts            Payout domain (pure + testable): rankPool/prizeForRank/computeWindowPayouts (one winner per paying track = top verified FINISHING run; no finisher → no payout) / closeWindow / PayoutRepo + createSupabaseRepo. **P7.1: pool = top-10 RAW-only (fetchPoolTracks .eq mode raw); DEFAULT_PAYOUT_TIERS poolSize 10, ladder 0.2 / 0.1×4 / 0.05×5 (max 0.85)**. windows.ts cron calls closeWindow.
    src/routes/adminPayouts.ts X-Admin-Key gated (P7, registered under /api/admin alongside admin.ts): GET /payouts/pending (status=pending + wallet/amount/label, total) / POST /payouts/:id/paid {txSig} (**now delegates to payoutOps.markPayoutPaid — shared with the TG bot; 400 bad-sig/404 not-found/409 not-pending**) / /payouts/:id/skip {reason} (skip_reason) / GET /runs/flagged / POST /runs/:id/approve|reject (clear a held flag → verified/failed) / GET /windows (history totals + unpaid). No private keys server-side.
    src/routes/auth.ts        POST /signup (performSignup → cr_players {username,pin_hash,wallet_address} + JWT; 400/409 errors) / /login (per-IP throttle 20·15min in-memory → performLogin → JWT, or 401 wrong / 403 banned / 423 locked w/ minutes). Wallet bound once + immutable (no change-wallet route anywhere). (P6 nonce/verify/register removed.)
    src/routes/runs.ts        POST /submit (requireAuth, P6/P6b/P7.4): fast rejects (banned 403 / simVersion 400 / min_run_time_ms 400 / inputLog>90000 400 / 1-per-10s 429) → **queue-cap 503 (MAX_VERIFY_INFLIGHT 25, M4)** → **load track + reject non-active 409 (M7)** → reserve slot (try/finally) → getOrCreateOpenWindow (**upsert onConflict starts_at + status='open' guard; closed→null window, M3**) → insert pending → **validate ticks (400) + compute ceiling from world_length** → runSerial(verifyRun{submittedTicks:body.ticks, client:{clientScore,flips,finished,timeMs}, maxScore:ceiling}) → **per-run budget: durationMs>2s ⇒ failed (M4)** → store **CLIENT's** verify_status+server_score(=clientScore, official; null on failed)+finished+flips+crashes (P7.4: client is authoritative) → ranks (verified∧finished only). server_score column still the ranking number all downstream reads.
    src/routes/leaderboards.ts (P7) GET /payout-board (Home centrepiece: rankPool top-20 + each track's current-window leader + prize + label + window ends_at) / GET /:trackId/global?scope=alltime|window (topPerPlayer of verified+finished) / GET /:trackId/me (requireAuth: player's best-5 + all-time rank).
    src/routes/payouts.ts     (P7) GET /paid: public receipts feed (status=paid → paidAt/label/username/amountSol/txSig + **kind** (P7.6: 'window'|'daily', tagged on #/payouts)). The trust flywheel; no auth.
    src/routes/daily.ts       (P7.6) GET /api/daily: today's open challenge → {date, trackId, label, points (Home sparkline), prizeSol, endsAt, top5 (verified+finished in-window via topPerPlayer)}. Graceful empty when none open. Public, no auth.
    src/routes/runs.ts (P7)   GET /:runId/replay (PUBLIC): {trackId, inputLog, username, label, serverScore} for read-only replay-watch.
    src/routes/*.ts           payouts plugin stub ({todo:true}).
    sql/001_track_pipeline.sql Reference DDL for live schema + hardening (freeze trigger, unique indexes, period-constraint widening). Owner applies in Supabase SQL editor.
    sql/002_terrain_tiers.sql DESTRUCTIVE tier migration (P4.2): tier column + indexes + freeze-guard. (superseded by 003.)
    sql/003_periods_savage.sql + 003_clean.sql  DESTRUCTIVE (P4.5): wipe maps/tracks, tier→(VOLATILE,DEGEN,SAVAGE), period→(1Y,6M,3M), reprice prize_ladder. Owner pastes 003_clean.sql BEFORE reseeding.
    sql/004_difficulty_payouts.sql + 004_clean.sql  NON-destructive (P4.6): add cr_tracks.difficulty_score (NOT frozen) + cr_runs.finished + cr_payouts unique(window_id,track_id) + cr_config.payout_tiers (poolSize 20, rank rules 0.2/0.1/0.05). Owner pastes 004_clean.sql, THEN run backfill.
    sql/005_accounts.sql      NON-destructive + idempotent (P6-rev): cr_players += pin_hash + failed_attempts + locked_until; unique(wallet_address) (claimed once) + unique(lower(username)). Owner pastes in Supabase. cr_auth_nonces left unused.
    sql/006_security.sql      NON-destructive + idempotent (P6b): cr_players += lockout_count; **cr_register_login_failure(uuid)** plpgsql fn (row-locked atomic increment + escalating lock — must mirror evaluateLogin); unique(cr_payout_windows.starts_at). Owner pastes in Supabase (login fails until applied — the RPC must exist).
    sql/007_payouts_admin.sql NON-destructive + idempotent (P7): cr_payouts += skip_reason (admin SKIP). Status 'skipped' is app-level; widen a status CHECK if one exists (commented).
    sql/008_raw_pool.sql      (P7.1) update cr_config.payout_tiers → {poolSize 10, ladder 0.2/0.1×4/0.05×5}. RAW-only filter is in app code. Owner pastes.
    sql/010_daily_challenge.sql  (P7.6, owner pastes) cr_daily_challenges table (one row/UTC day: track_id, challenge_date unique, starts/ends_at, status open|settled, winner_player_id/score, paid) + cr_config.daily_prize_sol=0.5 + extend cr_payouts (kind 'window'|'daily', daily_challenge_id FK, window_id nullable, partial unique on daily_challenge_id). Non-destructive + idempotent.
    sql/009_status_constraints.sql  **CRITICAL, APPLIED** — widen the stale dashboard CHECK constraints that never permitted the app's status vocabulary (the real root cause of "runs never verify / windows never close / no payouts"): cr_runs.verify_status → pending/verified/flagged/failed; cr_payout_windows.status → open/settled/closed; cr_payouts.status → pending/paid/skipped. Non-destructive + idempotent.
    scripts/seed-maps.ts      Seeds 6 coins × 3 periods → 108 tracks via admin HTTP. `npm run seed -w @chainrider/api`.
    scripts/backfill-difficulty.ts  Backfills cr_tracks.difficulty_score for all tracks from stored points (reads points, writes only difficulty_score → passes freeze guard; idempotent). `npm run backfill -w @chainrider/api` (after sql/004).
    scripts/gen-golden.ts     Regenerates golden strings for trackgen tests (only on deliberate algorithm changes).
    test/{trackgen,payouts,runVerify,accounts,auth,leaderboards,telegram,daily}.test.ts  Vitest (102): **daily (dayBoundsUTC UTC-midnight bounds; pickRandomTrackId in-set + draws across 108; pickDailyWinner top-score/time-tiebreak/empty)**; **telegram (buildPayoutNotification incl. silent-on-empty; buildDailyWinnerNotification; buildPendingList; parsePaidCommands; isValidSolanaSig; markPayoutPaid paid/not_found/not_pending/bad_sig via fake db; handlePaid solscan reply)**; trackgen; payouts; **runVerify P7.4 trust-client model (well-formed+progress+plausible→verified at client score; empty log→flagged no-progress; real-but-tiny over-claim→flagged implausible; malformed→failed; over-ceiling→flagged; maxPlausibleScore scales with ticks + clears real runs; isWellFormedLog; maxReasonableScore generous)**; accounts (PIN-hash; H1 concurrency; M6); auth HS256 JWT; leaderboards.
    .env.example              SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, JWT_SECRET, ADMIN_KEY, COINGECKO_API_KEY, **TELEGRAM_BOT_TOKEN, TELEGRAM_ADMIN_USER_ID** (both required to enable the payout bot; unset = disabled).
scripts/
  dev.mjs                     Zero-dep concurrent runner for root `npm run dev` (api + web).
  launch/                     PumpPortal token launch — typed launchToken stub only.
deploy/                       Empty placeholder for hosting/infra config.
```

## Status

_Update at the end of every session._

**Done**
- Research phase: `docs/PHYSICS_RESEARCH.md` (Planck wheel-joint rig, fixed-timestep loop, determinism checklist).
- Research phase: `docs/ART_RESEARCH.md` (code-drawn vector neon bike chosen; skin config; draw order).
- This `CLAUDE.md` established.
- Monorepo skeleton (2026-06-11): 4 workspaces wired and type-checking (`npm run verify` clean). Physics builds dual ESM/CJS via tsup with `planck@1.5.0` pinned exact; `createSim`/`stepSim`/`getSnapshot` create a real stepping world with chain terrain (no bike rig yet). Web shows placeholder grid + pings `/api/health` through the Vite proxy (verified end-to-end). API serves health + six `{todo:true}` route-plugin stubs. Root `npm run dev` uses zero-dep `scripts/dev.mjs` (no `concurrently`). First commit made; repo-local git identity set.
- Dev ports pinned (2026-06-12): web dev server fixed to **:5180** with `strictPort: true` in `apps/web/vite.config.ts` (5173 is used by another project on this machine); api stays on :8787.
- Real physics + playground (2026-06-12): full deterministic sim in `packages/physics` — Appendix A rig (chassis box + 2 bullet wheels + WheelJoints + head sensor), X-Moto lean, edge-triggered jump (bit 16), crash (head sensor + swept circle + hard-landing impulse∧angle + kill floor 30m below min), 60-tick freeze → checkpoint respawn (every 15% of span), full scoring (airtime 10/6t flat; flips ±2π=250; combo ×1→×5 over 120t on flips/clean landings/wheelies; clean landing 50 within 30°+10t pair; wheelie 20/60t @>2m/s; crash −100 clamp≥0; finish 1000 + (par−t)/100), `simulateReplay` + `SIM_VERSION=2`. Track = chart + 20m lead-in + 30m run-out, finish flag at lastX+10. Input log = change-only `[tick,mask]`, mask persists. Web playground: accumulator loop, tune sliders (live rebuild), HUD, determinism self-test (600 live ticks vs replay → PASS in Node parity test). `npm run verify` clean.

- Tuning pass (2026-06-12): **locked tune v1** is the new `DEFAULT_TUNE` (chassisDensity 10, attitudeTorque 70, wheelRadius 0.34, groundFriction 1.45, etc. — found in the playground; DEFAULT_TUNE in `packages/physics/src/types.ts` is now the source of truth, Appendix A keeps the original research starting values). New tune param `chassisSpinCap` (default 6.5 rad/s): chassis angular velocity clamped to ±cap **only while fully airborne**, applied post-step; grounded dynamics untouched. `SIM_VERSION=3`. Verified: `npm run verify` clean + bit-identical double replay in Node.
- Wheelie recovery assist (2026-06-12): new tune param `wheelieRecoveryBoost` (default 1.7) — applied attitude torque is multiplied by it while holding lean-forward with rear wheel grounded + front airborne (decay state not boosted; all other states unchanged). Slider added. `SIM_VERSION=4`. Verified: verify clean + bit-identical double replay.
- **P2 complete — tune locked** (2026-06-12): final DEFAULT_TUNE locked from the playground (only delta from v1: attitudeMin 5.5 → **8.5**; wheelieRecoveryBoost 1.7 + chassisSpinCap 6.5 included). `SIM_VERSION=5`. Verified: verify clean + determinism smoke ×10 bit-identical.

- **P2.1 — arcade grounded stabilization layer** (2026-06-12): four grounded-only assists in `stepSim`, every one gated on ≥1 wheel grounded (fully-airborne ticks run the pre-P2.1 code paths — locked air feel untouched): (1) PD auto-stabilizer pulling chassis toward the direction-averaged terrain slope under the wheels (`stabilizerStrength` 90, `stabilizerDamping` 12; any lean input drops authority to 30% so deliberate wheelies/manuals win; off during crash freeze), (2) motor torque taper — full to 40% of maxOmega, then linear to `torqueFalloffFloor` 0.35 at maxOmega, (3) hill traction assist — throttle uphill >15°: force along surface = `hillAssist` 0.45 × m·g·sin(slope); zero downhill/in air, (4) anti-wheelie bias — grounded throttle torque scaled linearly to `antiWheelieFloor` 0.4 between 25°→50° nose-up vs slope, bypassed entirely while lean-back held. `maxMotorTorque` 60 → **41**. `SIM_VERSION=6`. New vitest suite in `packages/physics/test/` (vitest devDep). Verified: verify clean, tests pass (climb reaches top with ≤8° pitch error), 10× dist replay bit-identical, flat full-throttle max pitch 9.5° vs deliberate wheelie unrestricted.

- **P3 — track pipeline** (2026-06-12): full chart→track pipeline in `apps/api`. Pure deterministic `trackgen.ts` (downsample ≤1000 candles for ALL, normalize 6m/candle + vol-scaled 25–90m band + 55° slope clamp via single global y-rescale, centripetal Catmull-Rom smooth mode resampled 1 vtx/m with monotonic-x guard, stats/difficulty easy<20°/med<32°/hard<45°/insane≥45° from raw) — golden-tested byte-identical (21 vitest tests). `chartdata.ts` fetchers (CoinGecko + GeckoTerminal, retry ×3). Routes: GET /api/maps, GET /api/tracks/:id, admin POST /maps + /maps/:id/regenerate (X-Admin-Key, fail-closed). Discovered the **live Supabase schema pre-exists** (integer ids; cr_tracks has FLAT stats columns point_count/world_length/max_slope_deg/volatility/par_time_ms, NOT jsonb; cr_config prize_ladder is per-difficulty arrays) — code adapted to it; `sql/001_track_pipeline.sql` is reference + optional hardening. **Period vocabulary is uppercase `90D|180D|1Y|ALL`** (live cr_maps check constraint currently allows only 1Y/ALL; widening statement in sql file). Seeded + verified live end-to-end: btc-1y (regenerated to v2; v1 frozen-but-servable confirmed), eth-1y, sol-1y. Verify + tests clean.

- **P4/P5 — playable game UI** (2026-06-13): hash-routed SPA in `apps/web` (router + screen lifecycle replacing the single-canvas mount; playground refactored to `startPlayground(root)→{unmount}` under `#/playground`). **Home** (`#/`): hero, live stats strip from new `GET /api/stats`, trending cards (per-map sparkline from raw track points, difficulty badge, rank-1 SOL prize), UTC-aligned 30-min payout countdown (client-computed). **Map detail** (`#/map/:slug/:period`): full chart preview (green-up/red-down + fill), period tabs (siblings grouped by symbol), RAW/SMOOTH toggle, stats row + prize ladder, top-10 leaderboard (empty-state; `GET /api/leaderboards/:trackId` stubbed `[]`), RIDE button. **Ride** (`#/ride/:trackId`): fetches frozen points → `createSim` → P2 fixed-timestep loop; camera with smoothed lookahead + speed zoom (1.0→0.8) + kill-floor clamp; terrain = the chart, **culled via binary-search `visibleRange`** so per-frame work is bounded by visible segments (verified: 40m window over a 1255-pt track touches <15 segments → offscreen-cache fallback not needed); green/red glow + gradient fill + gridlines; DOM HUD (score/combo/air/clock/minimap/legend); change-only `[tick,keymask]` input log (the P6 replay), 20-min cap; run-complete card (star rating = `STAR_FRACTIONS` × `max_score_per_track_default`, flips/crashes/combo/time grid, Submit posts real payload to stub `POST /api/runs/submit`, Retry, New Track). Zero physics/scoring in web (shared `drawBike` is the only extracted render logic). Verified: `npm run verify` clean (all 4 workspaces), web `vite build` clean (29 modules), API endpoints return correct shapes, dev-server `/api` proxy works end-to-end, terrain-culling logic check passed. **Not browser-verified** (no automation tool in this env): live rendering/camera feel + the actual ride loop need a manual pass at `http://localhost:5180/#/`.

- **P4.1 — crash on head contact only** (2026-06-13): removed the hard-landing-impulse crash trigger from `stepSim` (old condition D) and the post-solve chassis/wheel impulse accumulator that fed it (plus the now-dead `tickMaxImpulse`/`headContact` Sim fields). Crash now fires ONLY on head-vs-track contact — `isHeadTouching` (head sensor) or `sweptCircleHitsTerrain` (swept head circle) — or `pos.y < killY` (the void). The chassis box + wheels resting/grazing/bottoming on terrain at any angle or speed is never a crash; the box stays a solid colliding fixture. `landingAligned` kept (scoring's clean-landing detector still uses it); `hardLandingImpulse` left in `BikeTune` (unused by sim, still a panel slider). `SIM_VERSION=7`. New vitest regression: an 8 m drop landing wheels-down keeps `crashes===0`. Verified: verify clean (all 4 workspaces), 3/3 physics tests pass incl. bit-identical replay.

- **P4.2 — time-primary scoring + terrain tiers + DOGE** (2026-06-14): bike tune LOCKED (unchanged). **(A) Scoring** reworked time-primary in `scoring.ts`: `score = speedScore + trickBonus`, `speedScore = round(10000·(par/effectiveTime)^1.5)` at finish only, `effectiveTime = finishTime + crashes·3000ms`, `trickBonus = round(rawTrickPoints·0.15)`; DNF = trickBonus only. Crashes no longer subtract points (they cost time); trick detection unchanged (feeds rawTrickPoints). `SCORING_CONFIG` holds all knobs incl. `parPaceMps {CHILL:9,VOLATILE:8,DEGEN:7}`. `computeFinalScore` is a pure exported fn (anti-exploit tested: fast clean > slow+flips+crashes; DNF < finish). Snapshot/FinalResult expose speedScore/trickBonus/effectiveTimeMs. `SIM_VERSION=8`. **(B) Terrain tiers** in `trackgen.ts`: amplify(deviation from mean line ×factor) + roughness(seeded hashPoints→mulberry32, NEVER Math.random) + per-segment 55° clamp (replaces global rescale for tiers → sustained ~55°, not one spike). `generateTier`: CHILL≡normalize byte-identical (existing goldens intact), VOLATILE ×1.8/0.4, DEGEN ×2.8/0.9. Each (map,tier,mode) = a frozen track with per-tier par. `/api/maps` additive: per-map `tiers{}` + legacy difficulty/tracks (from VOLATILE) so the pre-tier UI keeps working; prize_ladder tier-keyed (CHILL/VOLATILE/DEGEN). DOGE added (uncorrelated). Run-complete card shows speed/trick split + time+crash-penalty. Verified: `npm run verify` clean, 8/8 physics + 27/27 api tests pass, real-data smoke (BTC/DOGE 1Y): steep-segment density escalates CHILL→VOLATILE→DEGEN (BTC 1→3→30, DOGE 1→6→20 of 364) and DOGE differs from BTC. **NOT yet applied live** (no Postgres DDL channel here): owner must paste `sql/002_terrain_tiers.sql` then `npm run seed -w @chainrider/api` → 24 tracks.

- **P4.2 applied live** (2026-06-14): owner pasted `sql/002` (+ a follow-up `alter table cr_tracks drop constraint cr_tracks_map_id_mode_version_key` — a pre-existing dashboard constraint that omitted tier; now folded into the 002 files). Seeded 4 coins → **24 tracks** verified via `/api/maps` (per-tier par 243/273/312 s, DEGEN steepest). Migration fix committed as P4.2a.

- **P4.4 — fix checkpoint respawn clipping** (2026-06-14): respawn placed the chassis from terrain height at the checkpoint x only, but `setBikePose` puts the wheels at `x ± wheelbase/2` where, on a slope, the uphill wheel's terrain is `≈halfBase·tan(slope)` higher → that wheel spawned under the surface (≈0.61 m at 40°). Fix in `buildTrackInfo`: checkpoint Y now lifts above the **max terrain under the whole bike footprint** (`maxTerrainBetween` over `x ± max(wheelbase/2, chassisWidth/2)`, including any peak vertex) + wheelRadius + axleDropY + `SPAWN_CLEARANCE` (0.02 → **0.3 m**), so both wheel bottoms clear their local terrain on any slope and the bike drops/settles. Upright, zero velocity unchanged. `SIM_VERSION=9`. New `respawn.test.ts` (both wheels above terrain at every checkpoint on a 40° incline); verified on real BTC DEGEN smooth track (worst gap exactly 0.30 m, 0 penetration). verify + 9/9 physics tests pass.

- **P4.3 — tier selection UI** (2026-06-14): Home/MapDetail now drive all three tiers from `map.tiers[]` (no physics/scoring change). **Home** cards: removed the stale legacy badge; each card = a coin link (symbol + neutral-cyan sparkline from the VOLATILE track) + a row of **3 prized tier chips** (CHILL mint 0.02 / VOLATILE amber 0.05 / DEGEN red 0.12, DEGEN glowing), each deep-linking to `#/map/:slug/:period/:tier`. **MapDetail**: new TIER selector (primary, above RAW/SMOOTH) defaulting VOLATILE; tier × mode → `map.tiers[tier][mode]` drives chart preview, stats row + tier badge, full prize ladder, leaderboard (still P7 empty-state, keyed to the selected track), and RIDE target — all 6 tracks/coin reachable. `tierColor` added to `ui/format.ts`; router gains a 4-seg tier deep-link route. Reverted the temp `LEGACY_TIER=DEGEN` hack back to VOLATILE. Verified: `npm run verify` clean, web `vite build` clean, `/api/maps` carries per-tier prizes + raw/smooth trackIds for all 24. **Single-player feature-complete.**

- **P4.5 — auto-submit, SAVAGE tier, low-speed launch, multi-period, memecoins** (2026-06-14): **(A)** run-complete card AUTO-SUBMITS on end (finish/quit) with a Saving→Saved/Failed status; Submit button removed (Retry/New Track kept). **(B)** Home decluttered → one card per coin (no tier chips). **(C)** tier ladder dropped CHILL, now VOLATILE/DEGEN/SAVAGE (new hardest: amplify ×3.6, roughness 1.2, par pace 6, magenta `#e23bff`); prizes all paid+shifted (VOLATILE [0.03,0.015,0.008] / DEGEN [0.07,0.035,0.015] / SAVAGE [0.15,0.08,0.04]). **Low-speed launch assist** in `stepSim` (the ONLY handling change): grounded+throttle+preSpeed<3m/s → motorTorque×1.8 scaling to ×1 at 3m/s, zero above — makes steep standstills recoverable, normal riding untouched. `SIM_VERSION=10`. **(D)** periods 1Y/6M/3M (CoinGecko 365/180/90d); `PERIOD_AMPLITUDE {1Y:1,6M:1.25,3M:1.5}` multiplies tier amplify so 3M is wildest; `generateTier(closes,tier,periodAmp=1)` keeps VOLATILE/DEGEN goldens byte-identical. **(E)** +2 memecoins POPCAT/BONK (data-validated best sustained rolling vol of WIF/PEPE/BONK/POPCAT; WIF unfetchable, PEPE quietest 3M). Verified: verify clean, 11/11 physics + 28/28 api tests, web build clean. **Applied live** (2026-06-14): owner pasted `sql/003_clean.sql`, seeded **108 tracks** confirmed via `/api/maps` (6 coins × 3 periods × 3 tiers, no CHILL).

- **P4.6 — steepness-graded payout pool (top 20 tracks)** (2026-06-14): no physics/frontend gameplay change. **(1)** `stats()` now returns deterministic `difficultyScore` = steep-segment density (>35°) + avg-slope×1e-6 tiebreak (round8); on cr_tracks via new `difficulty_score` column (NOT frozen → re-gradable); `scripts/backfill-difficulty.ts` recomputes it for all 108 from stored points (reads points, writes only the score → freeze guard untouched). **(2)** Paying pool = rule-based top-20 by score (`cr_config.payout_tiers`: poolSize 20, ranks 1/2-10/11-20 → 0.2/0.1/0.05 SOL; max 1.6/window); re-grading reshuffles automatically, no hardcoded ids. **(3)** `GET /api/payout-pool` returns the live top-20 with rank/prize/coin/period/tier/mode. **(4/5)** `payouts.ts`: pure `rankPool`/`computeWindowPayouts` + `closeWindow(repo,windowId)` — per paying track, the winner is the top VERIFIED FINISHING run (highest server_score); **a track with no finisher pays nothing** (no dead-window bleed); idempotent (app-level paid-track filter + DB `unique(window_id,track_id)`); per-track independent (a player can win N tracks → N payouts). Needs `cr_runs.finished` (added in migration; P6 re-sim populates it). `SIM_VERSION` unchanged. Verified: verify clean, 37/37 api tests (points goldens byte-identical), and a read-only report from stored points → **exact top-20 confirms the design**: all 20 are 3M; SAVAGE 12 / DEGEN 8; #1 = POPCAT 3M SAVAGE; total 1.6 SOL. **Applied live** (2026-06-14): owner pasted `sql/004_clean.sql`, backfilled all 108 (0 failures), and `/api/payout-pool` reproduced the exact top-20 (1.6 SOL, all 3M, SAVAGE 12/DEGEN 8, POPCAT 3M SAVAGE #1).

- **P4.7 — context-aware reverse** (2026-06-14): S/down (the brake bit) is now context-aware in `stepSim` — the ONLY handling change, all other tune locked. Moving forward (`forwardVel > reverseEngageSpeed` 1.5 m/s, chassis-forward projection) OR airborne → **brakes exactly as before** (byte-identical: rear motor 0 + rearBrakeTorque, front brake). Grounded + at/below crawl → **reverses**: rear motor positive (=backward) at `reverseMotorTorque` 30 / `reverseMotorSpeed` 12, thrust cut above `reverseMaxSpeed` 4 m/s; never airborne (no reverse thrust in air). Smooth (reverse motor decelerates residual forward roll then backs up — no snap). 4 new BikeTune fields + playground sliders. `SIM_VERSION=11`. New `reverse.test.ts` (forward brake decelerates > coast & stays forward; standstill S for 2 s backs up >3 m, grounded, capped). Verified: verify clean, 13/13 physics tests incl. bit-identical determinism replay.

- **P4.9 — reverse incline traction for steep-valley escape** (2026-06-15): P4.7 reverse couldn't climb out of a steep V — the rigid Box2D rear wheel slips on a 40-45° wall and the motor just spins. Fix (ONLY handling change; forward throttle/brake/lean/jump/suspension byte-identical): **(1)** bumped reverse power — `reverseMotorTorque` 30→60, `reverseMaxSpeed` 4→5.5, `reverseMotorSpeed` 12→17 (so 5.5 is reachable). **(2)** REVERSE INCLINE TRACTION ASSIST (the real fix, mirrors forward hillAssist): reversing + grounded + backing up a slope `< -reverseHillMinSlopeDeg`(15°) → apply a **chassis surface force** `reverseHillAssist`(0.5)·m·g·sin(slope) up-slope-and-backward. A body force is grip-independent, so rear-wheel micro-bounce no longer matters — chose this over scaling motor torque (which just spins a slipping wheel faster). **(3)** wheel restitution already 0 (default) — confirmed, no suspension change (preserves forward feel). 2 new BikeTune fields + sliders. `SIM_VERSION=12`. New steep-V escape test (settles at the bottom of a 45° V, holds reverse → climbs back up the entry wall; assist climbs dy≈8m vs no-assist control's ≈2m). Verified: verify clean, 15/15 physics tests incl. bit-identical determinism replay.

- **P6 — wallet accounts + server-verified runs** (2026-06-15): no physics/scoring change (`SIM_VERSION` stays 12; `packages/physics` imported + re-run as-is). **Auth** (`apps/api/src/auth.ts` + `routes/auth.ts`): wallet = account = payout address, no passwords/email. `/auth/nonce` (upsert `cr_auth_nonces`, 5-min single-use) → client signs `"CHAINRIDER login\nwallet:…\nnonce:…"` with Phantom → `/auth/verify` (tweetnacl+bs58 ed25519; existing player → 7d JWT `{playerId,wallet}` + last_login_at; new wallet → `{needsUsername}`, no row) → `/auth/register` (fresh nonce + re-sign + username 3-16 [a-z0-9_], case-insensitive unique → `cr_players` + JWT). No private key ever server-side. **Web** (`apps/web/src/auth.ts`): direct `window.solana` (no wallet-adapter — vanilla TS), persistent `#wallet-bar` mounted to `<body>` (survives router replaceChildren), username modal, JWT in localStorage, `apiFetch` auto-attaches `Bearer`. **Run submit** (`runVerify.ts` pure + `routes/runs.ts`, requireAuth): fast rejects (banned 403 / simVersion 400 / min_run_time_ms 400 / inputLog>90000 400 / rate-limit 1·10s 429) → bucket into UTC-aligned 30-min `cr_payout_windows` (get-or-create) → insert `cr_runs` pending → **serial-queued `verifyRun`** re-runs `simulateReplay(DEFAULT_TUNE)` on FROZEN points → compares (exact: score ±1% ∧ flips/crashes/finished == ∧ time ±250ms → 'verified'; ≤±5% → 'flagged'; worse/throw → 'failed') → stores `verify_status`+`server_score`+`finished` (server authoritative) → ranks (verified∧finished only) → `{verifyStatus,serverScore,rankThisWindow?,rankAllTime?}`; run-complete card shows VERIFIED ✓ + ranks / "Couldn't verify" / "Connect wallet". **server_score is the only number that ranks/pays.** No schema migration (columns pre-exist from P4.6). Verified: `npm run verify` clean (4 ws), web `vite build` clean, **52/52 api tests** (new anti-cheat: good→verified, +5000 client→never verified, tampered log→server_score≠claim, DNF→no rank, 1255-pt replay <1s; auth sig-roundtrip) + 15/15 physics unchanged. **NOT browser-verified** (no automation here): live Phantom connect→sign→register→ride→VERIFIED flow needs a manual pass + a real `cr_runs` row check. **(Auth layer superseded by P6-rev below; the run-verification engine here is unchanged.)**

- **P6-rev — username + PIN + wallet accounts (replaces wallet signing)** (2026-06-15): swapped the P6 Phantom/signature identity layer for **username + 4-digit PIN + a pasted Solana payout address**, bound at signup. **The P6 server-verified run engine is untouched** (`runVerify.ts`, `simulateReplay`, scoring, `/api/runs/submit`, the run-complete VERIFIED/rank UI — only the JWT now comes from PIN login). `SIM_VERSION` 12. **Hashing = `bcryptjs`** (pure-JS, no node-gyp on Windows; cost 10); **tweetnacl removed**. **`accounts.ts`** (pure + repo-injected): `validatePin`/`validateWalletAddress` (bs58→32 bytes)/`hashPin`+`verifyPin`/`evaluateLogin` (5 wrong → 15-min lock; expired lock self-clears; active lock rejects even a correct PIN)/`performSignup`+`performLogin`/`AccountRepo`. **`routes/auth.ts`**: `/signup` (validate + server-side confirm-equality + username/wallet uniqueness → `cr_players` + JWT) and `/login` (per-IP throttle 20/15min + per-account DB lockout → JWT / 401 / 403 / 423-with-minutes); P6 nonce routes removed. **Lockout state on `cr_players`** (failed_attempts/locked_until) not a side table — per-account, single-row, no join. **Web `auth.ts`** rewritten (no `window.solana`): Log In + 2-step Sign Up modals (the 2nd step shows the wallet BIG + clipboard-swap warning + a required "I've checked" checkbox gating Create — the wallet is **permanently immutable**, no change-wallet route exists for anyone). `apiFetch` now surfaces the server `{error}` (lockout text). **Schema** = `sql/005_accounts.sql` (owner pastes: pin_hash + lockout cols + **unique(wallet_address)** claimed-once + unique(lower(username)); non-destructive/idempotent; `cr_auth_nonces` left unused). Security: PIN **always** bcrypt-hashed (tested: stored value is a `$2…` hash, never plaintext); brute-force lockout durable + unit-tested with injected `now`; wallet validated/unique/immutable; no private keys anywhere. Verified: `npm run verify` clean (4 ws), web `vite build` clean, **67/67 api tests** (P6 `runVerify` fixtures unchanged + new accounts/auth: signup rejections, lockout+unlock, PIN-is-a-hash) + 15/15 physics unchanged. **Owner TODO**: paste `sql/005_accounts.sql` in Supabase, then a manual Sign Up → Log In → ride → VERIFIED pass (+ wrong-PIN ×5 lockout check).

- **P6-rev2 — ride-gated auth + persistent session + hide bar in ride** (2026-06-15): web-only UX. **Gate riding, not browsing**: Home/MapDetail/leaderboards/payout board are fully browsable logged-out (no forced login on arrival); the **RIDE button** (`mapDetail.ts`) and a **direct `#/ride/:id` deep-link** (`ride.ts` mount, wrapped in `proceed()`) now call `requireLogin(onSuccess)` — logged-out opens the Login/Sign-Up modal and **continues into the ride** on success; logged-in rides immediately. Server already requires JWT to submit. **Value prop visible**: wallet-bar tagline "Log in to ride & win SOL" + a hint under the RIDE button when logged-out. **Persistent session**: unchanged localStorage JWT survives all nav + hard refresh (no re-prompt); `isLoggedIn()` self-clears an expired JWT (`isTokenExpired` decodes `exp`). **Hide auth bar during a ride**: `#wallet-bar` `display:none` on `#/ride/*` via a hashchange listener in `mountWalletButton` (reappears when the ride ends). Verified: `npm run verify` clean (4 ws) + web `vite build` clean. Browser pass still owner's (logged-out browse → RIDE opens modal → into ride; logged-in nav/refresh persists; bar gone mid-ride, back after).

- **P6b — security hardening** (2026-06-15): adversarial review (`docs/SECURITY_P6.md`) → applied 7 fixes (skipped enumeration/CORS/dead-nonce-table as accepted minors). **H1 (critical) atomic lockout**: the old read-bcrypt-write let concurrent wrong-PINs lose `failed_attempts` updates and bypass the lock; now `performLogin` delegates the increment to **`cr_register_login_failure(uuid)`** — a `for update` row-locked plpgsql fn (sql/006) — so concurrent failures can't be lost (`evaluateLogin` is the reference math the SQL mirrors). **M6 escalating lockout**: each consecutive lockout doubles (15→30→60min, capped 64×) via new `cr_players.lockout_count`, resets on success. **H2 JWT**: `verifyToken` pinned to `algorithms:['HS256']`; the API refuses to boot if `JWT_SECRET` <32 chars (`assertJwtSecretStrength` in index.ts) — owner's `.env` already 64 chars. **M5**: username lookups `.ilike`→`.eq` on lowercased value (underscore is a LIKE wildcard). **M3**: `getOrCreateOpenWindow` uses upsert `onConflict:starts_at` + a unique index (sql/006) so windows can't double-create, and a `status='open'` guard so a run can't attach to a closed window. **M7**: `/submit` rejects non-`active` track versions (409) before inserting. **M4**: bounded verify queue (`MAX_VERIFY_INFLIGHT=25` → 503) + per-run 2s budget (over-budget ⇒ failed); note the single-replay cost is already bounded by `MAX_REPLAY_TICKS` and isn't attacker-inflatable. No physics/scoring change; `SIM_VERSION` 12. Verified: `npm run verify` clean (4 ws), **69/69 api tests** (P6 anti-cheat fixtures UNCHANGED + new H1-concurrency + M6-escalation). **Owner TODO**: paste `sql/006_security.sql` in Supabase **before** the next login (the `cr_register_login_failure` RPC must exist, else wrong-PIN logins error).

- **P7 — windows + leaderboards + payout admin + public receipts** (2026-06-15): wired + surfaced the EXISTING P4.6 payout math (no math reimplemented; no new physics). **Window engine** (`windows.ts`): `startWindowEngine(app)` runs a boot catch-up (close elapsed + open current — crash recovery) then node-cron `"0,30 * * * *"` UTC → `closeElapsedWindows` (open windows past `ends_at` → the EXISTING `closeWindow()`) + `ensureOpenWindow` (extracted from runs.ts, shared). **SINGLE-INSTANCE ONLY** (in-process cron; double-close is safe via idempotency but don't run two). **Leaderboards** (`leaderboards.ts` pure `topPerPlayer`/`rankOfPlayer` + routes): `/payout-board` (Home centrepiece — top-20 pool + current-window leaders + window ends_at), `/:trackId/global?scope` (This Window/All-Time), `/:trackId/me` (PBs + rank). **Admin** (`adminPayouts.ts`, X-Admin-Key, under /api/admin): pending (wallet+amount), mark-paid(txSig)/skip(reason), flagged review + approve/reject, window history. **Flagged runs are inherently excluded from payouts** (closeWindow reads verified-only) — admin just surfaces/clears them. **Public receipts** (`/api/payouts/paid` + `#/payouts`) + **public read-only replay** (`/api/runs/:runId/replay` + `#/replay/:runId`, reuses the ride loop with a log-driven mask). Web: Home payout board (apex glow + one countdown), MapDetail GLOBAL+YOUR-PBs (re-query on tier/mode/period), admin/payouts/replay screens, `apiFetch` admin-key header. `stats.totalSolPaid` was already real. `SIM_VERSION` 12. Verified: `npm run verify` clean (4 ws), web `vite build` clean, **74/74 api tests** (+ new `leaderboards` pure tests; payouts/anti-cheat UNCHANGED). **NOT runtime-verified here** (no scheduler clock / live DB in this env): the unattended :00/:30 open→settle cron, the race→one-payout close, and the admin phone flow need an owner pass. **Owner TODO**: paste `sql/007_payouts_admin.sql`; run ONE API instance.

- **P7.1 — top-10 RAW paying pool + Home reorder** (2026-06-15): reused the payout math (no math change) — only narrowed the pool + repriced + reordered Home. **Pool = the 10 hardest RAW tracks** (smooth never pays): `fetchPoolTracks` + the `/api/payout-pool` query gained `.eq("mode","raw")`; `DEFAULT_PAYOUT_TIERS` → `poolSize 10`, ladder **1→0.2 / 2–5→0.1 / 6–10→0.05 (max 0.85/window)**; `sql/008_raw_pool.sql` resets the live `cr_config.payout_tiers`. `closeWindow`/`/payout-board`/`/payout-pool` all read `fetchPoolTracks`+`rankPool`, so they become raw top-10 automatically; per-track winner logic unchanged. **Smooth tracks keep their leaderboards** (bragging rights), just no prize. **Home**: trending coin cards now come FIRST, the payout board ("10 PAYING TRACKS") sits under them with the same current-window leader column (already wired to verified runs) + shared countdown. Verified: `npm run verify` clean (4 ws), web build clean, **74/74 api tests** (payouts updated: top-10, 0.2/0.1×4/0.05×5 sum 0.85; the 3-track close test moved its 3rd track from id 11→6 to stay in-pool). **Owner TODO**: paste `sql/008_raw_pool.sql`; confirm `/api/payout-pool` returns 10 raw tracks.

- **P7.2 — authoritative-server verification (fixes cross-engine determinism)** (2026-06-15): diagnosed why every run failed verify — **Chrome-V8 ≠ Node-V8 floating-point** (planck transcendentals aren't bit-identical across engines), so the old "server score ≈ client score (±1%/±5%)" gate was unachievable. **Confirmed read-only**: Node reproduces the stored server result byte-for-byte (divergence is browser↔Node, not data), and the old path also **over-ran** DNF replays to 72000 ticks. **Fix = authoritative-server model**: the server's re-sim of the inputLog is the SOLE truth. `runVerify.ts` rewritten — `verifyRun({points,parTimeMs,inputLog,submittedTicks,maxScore})`, NO client score: well-formed log → `simulateReplay` **capped at submittedTicks (Bug A)** → must `finished` (DNF→failed) → `score ≤ maxReasonableScore` (generous per-track ceiling from par+world_length, on-the-fly, no migration) else `flagged`. **Client score/flips/crashes are display-only**, never rank/pay/gate; the ±% comparison is gone. `runs.ts` validates `ticks` (400), computes the ceiling, stores the server's score/finished/flips/crashes (authoritative) + keeps `client_score` as the labeled estimate. Run-complete card labels the big number "(estimate)" and shows the official server score + rank on submit. **No physics change**; `SIM_VERSION` 12; Node↔Node determinism test untouched. Verified: `npm run verify` clean (4 ws), web build clean, **77/77 api tests** (new authoritative fixtures a–e + isWellFormedLog + ceiling). **Real-data check (read-only)**: re-ran stored runs 1–4 through the NEW path capped at their own tick counts → all four are genuine **DNFs** (`finished:false`, e.g. run 2 = 218 s on a 90 s-par track), so they correctly stay unverified — the player simply never reached the finish flag (which is why nothing showed). Bug A confirmed fixed (run 2 now caps at 13117 ticks / score 1913, not the over-run 8139). A real finishing ride will now verify at the server's authoritative score. **Known residual**: a marginal run whose finish flips to a crash under Node's float won't verify — only WASM/fixed-point physics would make it bit-exact (out of scope).

- **P7.2 follow-up — cross-engine divergence is CATASTROPHIC on hard tracks, not marginal** (2026-06-15, diagnosis only): a logged-in player finished **track 100 (SAVAGE raw)** in the browser (card showed FINISHED) but got "Couldn't verify". Diagnosed read-only on **run id 6**: Node's replay of the exact input_log (capped at submittedTicks, server points/tune confirmed identical — `/api/tracks/:id` returns points untransformed) reaches **only x≈49 of 550**, crashing **19×** starting at **tick 682 / x≈49** (an early steep feature the browser cleared), never passing the **first checkpoint (x=74)** so it's stuck near spawn — `finished:false`, `server_score 2253` (matches the stored value exactly → running server IS P7.2 code, divergence IS browser↔Node). So the "residual" isn't rare: steep contact-heavy terrain amplifies float divergence into an **early** crash, and the **paying pool is the 10 hardest RAW (SAVAGE/DEGEN) tracks** — exactly where divergence is worst. With planck-JS, legit wins on paying tracks essentially never verify. **Decision: NOT doing WASM** (too heavy). Instead → **P7.4** (below).

- **P7.4 — trust-client-score verification + plausibility filter + manual replay review (Option B)** (2026-06-15): replaces the P7.2 authoritative-server gate that couldn't verify hard-track rides cross-engine. **No physics/scoring change** (`SIM_VERSION` stays 12). **New acceptance model** (`runVerify.ts` rewritten, pure): the CLIENT's reported score is OFFICIAL (ranks + displays, never changes post-submit); the server replay is used ONLY to prove a real ride happened. `verifyRun({points,parTimeMs,worldLength,inputLog,submittedTicks,client:{score,flips,finished,timeMs},maxScore})` → (1) `isWellFormedLog` else **failed**; (2) `simulateReplay` capped at submittedTicks for its **maxX** → `progress = maxX − spawnX (=points[0].x−LEAD_IN/2)` < **`PROGRESS_MIN_METERS`(25)** ⇒ **flagged** (no real ride; does NOT require a finish — Option 3 fixed-distance gate, set LOW/generous so cross-engine early-crash rides like run 6 still pass while garbage logs that travel ≈0 m don't); (3) `client.score > maxScore` (ceiling) ⇒ **flagged**; (4) `client.score > maxPlausibleScore` (generous band sized by ticks + claimed-flips-capped-by-ticks, ×2 safety) ⇒ **flagged**; else **verified**. **`packages/physics`**: `simulateReplay`/`FinalResult` gained **`maxX`** (furthest forward chassis x; passive, non-behavioral — determinism `toStrictEqual` test untouched). **`runs.ts`**: passes client values into verifyRun; stores `verify_status` + `server_score`(=client score, official; `null` on failed) + `finished`/`flips`/`crashes` from the client; ranks (verified∧finished) on `server_score` (column unchanged so all downstream reads it). **Manual review is the anti-cheat**: admin PENDING rows get a prominent **WATCH REPLAY "review before sending SOL"** step (runId already exposed); flagged copy updated (no more "±5% of server score"). **Run-complete card**: score is official (no "(estimate)"), statuses VERIFIED ✓·#rank / "reach the flag to rank"(DNF) / "Under review"(flagged) / "Couldn't verify — invalid run"(malformed). **Run 6 (the SAVAGE finish that failed P7.2) now VERIFIES** by construction: spawn≈−10, Node-replay max-x≈49 → ~59 m travel ≫ 25 m gate → passes progress; well-formed, plausible, `finished` → verified + ranks. Verified: `npm run verify` clean (4 ws), web `vite build` clean, **79/79 api tests** (new runVerify a–e: verified-at-client-score / no-progress→flagged / real-but-implausible→flagged / malformed→failed / over-ceiling→flagged + maxPlausibleScore scales-with-ticks & clears real runs) + 15/15 physics unchanged. **No DB migration** (reuses server_score/finished/flips/crashes columns). **Owner TODO**: confirm run 6 verifies + appears on the leaderboard against the live row (no DB in dev env); a manual browser pass of a finishing ride showing VERIFIED ✓.

- **Live pipeline fixed + proven end-to-end** (2026-06-16): diagnosed why runs showed "saved" but never appeared — **stale dashboard CHECK constraints never permitted the app's status vocabulary** (`cr_runs.verify_status` rejected `'verified'` → runs stuck `pending`; `cr_payout_windows.status` rejected `'settled'` → windows never closed = **Bug 2's true root cause**, not just the dying `tsx watch` process). Fix = **sql/009_status_constraints.sql** (owner APPLIED). Also: `runs.ts` now logs loudly when a status write fails (was silently swallowed → client told "verified" while row stayed pending). Proven: ran the real pending run through `verifyRun` → `verified` (score 5888, 77 m progress) and it appears on the all-time board; restart → boot catch-up settled windows 1–22 (0 failures); window 22 closed with `inserted:1` → **first payout created** (SOL 3M DEGEN raw · @axle · 0.05 SOL · pending). **Run the API as ONE stable non-watch instance** (`npm run build && npm run start -w @chainrider/api`); `tsx watch` restarts mid-verify and breaks the flow.
- **Telegram payout bot** (2026-06-16, Option B notify-and-reply): `telegram.ts` + `payoutOps.ts` (see File map). On window close (cron or admin force-close) DMs `TELEGRAM_ADMIN_USER_ID` a copy-paste payout list (silent if none); owner replies `/paid <id> <txSig>` → shared `markPayoutPaid` flips the row to `paid` → auto-posts the `#/payouts` receipt; `/pending` lists unpaid. NEVER holds keys / sends SOL / auto-pays; obeys only the admin id; disabled if env unset. Started in `index.ts` (single-instance). Verified: verify clean (4 ws), **94/94 api tests** (+15 telegram). **Owner TODO**: fill `TELEGRAM_BOT_TOKEN` + `TELEGRAM_ADMIN_USER_ID` in `apps/api/.env`, then a live close→DM→/paid→receipt pass. **(Not committed yet — awaiting owner.)**

- **P7.6 — Daily Challenge** (2026-06-16): a daily contest layered on the existing system (no new infra). At 00:00 UTC the engine picks ONE random track from all 108 active (`pickRandomTrackId`); players' verified+finished runs on it that day rank on a daily top-5; at the next 00:00 the top finisher wins **0.5 SOL** (1st only, tunable `cr_config.daily_prize_sol`), a fresh track is picked. Daily runs are just `cr_runs` filtered by the day's track + `[starts_at,ends_at)` (no duplicate storage); the daily **payout reuses `cr_payouts`** (`kind='daily'`) so the **Telegram `/paid`+`/pending` and public `#/payouts` receipts work unchanged** (only a dedicated `buildDailyWinnerNotification`/`notifyDailyWinner` + a "DAILY" receipt tag added). `settleDaily` requires `finished=true`; no finisher → no payout. New `daily.ts` (engine, `0 0 * * *` UTC cron, single-instance, boot catch-up), `routes/daily.ts` (`GET /api/daily`), `sql/010` (owner pastes), Home **DAILY CHALLENGE** section under the payout board (track + sparkline + 0.5 SOL + 00:00-UTC countdown + top-5 + RIDE button). Verified: `npm run verify` clean (4 ws), web build clean, **102/102 api tests** (+8: daily pure helpers + daily TG message). **Owner TODO**: paste `sql/010`, then restart the API (boot creates today's challenge) and check `GET /api/daily` + the Home section; a finishing ride → daily top-5 → settle/force at day-end → daily payout DM.

- **P5 — neon light-cycle bike + glow trail + skins** (2026-06-16): replaced the placeholder bike (chassis box + circle wheels + stick rider) with an original Tron-INSPIRED (no Tron IP) neon vector light-cycle, drawn purely from the physics snapshot. **No physics/scoring change** (`SIM_VERSION` 12; render-only — crash slow-mo scales WALL-CLOCK pacing only, the SIM_DT tick stream + input log are byte-identical so replay/score are unaffected). New `shared/bike.ts` (3-pass `strokeNeon` glow; swingarm/forward-cant spine/exposed fork anchored to real hub screen positions + seat wedge + rider leaning ±15° to input, crouch on jump; wheels = ring + 3 spinning spokes; crash = red + ejected rider), `shared/trail.ts` (48-point rear-wheel ribbon, tapered additive, flip-land pulse), `skins.ts` (4 hex-swap skins MINT/VOLT/EMBER/MAGMA all default + localStorage + `createSkinPicker` + `prefersReducedMotion`). Ride renderer gained the trail + 8–12 crash spark particles + ~250ms shake; loop gained crash slow-mo (both motion effects off under `prefers-reduced-motion`). Skin picker on the run-complete card + map detail (persists). Playground shares the same bike + trail. Verified: `npm run verify` clean (4 ws), web `vite build` clean (37 modules). **Owner TODO (no browser here)**: visual pass — bike reads deliberate/original, wheels spin with speed, rider leans with A/D, trail reads in motion + a still, all 4 skins swap+persist, crash juice + reduced-motion, 60fps on the biggest track.

- **P5.1 — flowing neon chopper restyle** (2026-06-16): re-shaped `drawBike` only (no physics; same snapshot) to match the reference — long/low/stretched chopper silhouette via ONE continuous flowing bezier frame (tail overhang → low seat hump → forward-raked neck → nose past the front wheel) + faint belly fill; fork/swingarm stubs keep it anchored to the real hubs; rider re-drawn as a tucked-forward racer flowing line (lean still input-driven); thin elegant wheels (slim ring + small hub + refined spokes); rear trail thickened to a connected light-wall tail (head 11px). Same 3-pass glow + 4 skins. Verified: `npm run verify` clean (4 ws), web build clean. Owner: visual pass at :5180.

- **P5.3 — image sprite bike** (2026-06-17): replaced the code-drawn vector bike with a transparent PNG sprite (`apps/web/public/bike.png`, served at `/bike.png`). `drawBike` now draws the image centred on the chassis position + rotated to the chassis angle (leans/flips/tumbles with the physics, same anchor as before); tunable `SPRITE_WIDTH_M`/`SPRITE_OFFSET_X`/`SPRITE_OFFSET_Y` consts at the top of `shared/bike.ts`. Sprite preloaded on import + cached (graceful skip until decoded). **Trail unchanged** (code-drawn light-wall, still behind the sprite); crash particles/shake/slow-mo unchanged. Skins wired skinId→sprite path (all → the green PNG for now; per-skin coloured PNGs drop in later by swapping `Skin.sprite`). No physics change. Verified: `npm run verify` clean (4 ws), web build clean (bike.png copied to dist). **Owner: tune SPRITE_WIDTH_M/offsets to line the sprite wheels up with the terrain.**

- **P5.5 — 3-piece hybrid bike with wheel suspension** (2026-06-17): split the single sprite into frame+rider + front wheel + rear wheel, each drawn at its OWN physics body's position + rotation, so wheels spin and the suspension gap flexes over bumps (and all 3 tumble on crash). No physics change. `Skin.sprites = {frame, wheelFront, wheelRear}` (all → green set for now: `/bike-frame.png`, `/bike-wheel-front.png`, `/bike-wheel-rear.png`). Tunables in `shared/bike.ts` (FRAME_WIDTH_M ~4.5, FRAME_OFFSET_X/Y, SPRITE_ROTATION_OFFSET, FRONT/REAR_WHEEL_DIAMETER_M, per-wheel offsets). Trail/crash juice unchanged. Verified: `npm run verify` clean (4 ws), web build clean. **Owner TODO**: drop the 3 PNGs into `apps/web/public/` (those exact names) — until then the bike renders nothing (trail still shows); then tune the consts to seat wheels on the terrain + level the frame. (Old single `bike.png` now unused.)

- **P5.6 — dev bike-tuning slider panel** (2026-06-17): the 10 bike-sprite fit constants moved into an exported mutable `BIKE_TUNE` object in `shared/bike.ts` (drawBike reads it per frame). New DEV-ONLY `src/dev/bikeTunePanel.ts` (mounted in main.ts): hidden overlay toggled by `B` / `?biketune=1` with a slider per field (live updates while riding) + value readouts + COPY VALUES (object body for pasting back / baking). Inert until toggled; **remove before launch**. No physics/scoring/render-logic change. Verified: verify clean (4 ws), web build clean.

- **P5.8 — new frame art + dev body overlay** (2026-06-18): swapped in the finished `bike-frame.png`; added a `SPRITE_CACHE_BUST` query (`v8`) on sprite src so the browser re-fetches the new art (paths unchanged); reset frame tune to neutral for the new shape (FRAME_WIDTH_M 2.0, FRAME_OFFSET_X/Y 0, SPRITE_ROTATION_OFFSET 0; wheels unchanged 0.9/0). Kept 3-piece render + B tune panel + the **DEV-ONLY `J`/`?showbodies=1` collision-body overlay** (now committed). No physics change. Verified: verify clean (4 ws), web build clean.
- **P5.7 — floating-wheel Tron bike + rotation-aligned suspension** (2026-06-18): swapped in the new forkless art (sleek frame+rider PNG, no forks/wheels; two identical hub-centred glowing ring wheels) and reset `BIKE_TUNE` to neutral defaults (all offsets 0, frame 2.6m, wheels 0.9m) for re-dialing via the dev panel. The alignment logic was already correct from the prior fix (kept): each wheel draws at its OWN physics-body world position + spins by its body angle, and the frame/wheel offsets rotate with the chassis angle (not screen space) so nothing drifts through flips/wheelies/slopes; with no drawn forks the floating wheels just move with suspension — clean. No physics change. Verified: verify clean (4 ws), web build clean, new PNGs copy to dist. Owner: re-dial fit with the `B`/`?biketune=1` sliders, then send COPY VALUES to bake.
- **P5.7 — fix bike alignment under rotation + bake tuning defaults** (2026-06-17): baked the dialed-in fit into `BIKE_TUNE` (FRAME_WIDTH_M 2.35, ROT_OFFSET −0.195, wheel diam 0.88, front off +0.37/−0.02, rear off −0.3/+0.02, …). Bug fix: the per-wheel align offset was applied in SCREEN space, so it drifted when the bike rotated; now it's rotated by the chassis angle (each wheel still anchored to its own physics-body world position + spinning by its own angle), so pieces stay aligned at any rotation. At rest (chassisAngle≈0) it's identical to before → resting fit unchanged. Frame offset was already chassis-local (post-rotate). No physics change. Verified: verify clean (4 ws), web build clean. Dev tuning panel kept.

- **P8.1 — procedural synthwave soundtrack** (2026-06-19): Tone.js generative engine (`src/audio/music.ts` + `controls.ts`), no audio files. 4 tracks (mellow menu + euphoric/dark/fast ride), minor-key synthwave (pad/bass/16th arp/4-on-floor drums/side-chain pump), ~2-min arrangements auto-advancing; route-aware (menu vs ride pool), random ride track on entry. Autoplay-safe (first-gesture start, silent if `cr_muted`), master 0.35, corner mute+skip UI. Added `tone@15`. No physics/SIM_VERSION change. Verified: verify clean (4 ws), web build clean (Tone bundle >500kB warning only).

- **P8.2 — flip score popup** (2026-06-19): on flip completion, a floating neon "+N" spawns above the bike (world→screen anchored, tracks the camera), rising ~40px and fading over ~0.8s. N = the live trick-score delta the EXISTING scoring already awards (snapshot.score rises via rawTrickPoints on each flip) — read, not invented; confirmed flips already score, so no SIM_VERSION bump. render.ts only. Verified: verify clean (4 ws), web build clean.

- **P8.5 — unstick assist** (2026-06-19): fix rut-stalling by tuning EXISTING assist only — `hillAssist` 0.45→0.6 (forward uphill grunt) + `reverseHillAssist` 0.5→0.65 (reverse-rock out of a rut). NO `maxMotorTorque` change; launch params left at 1.8/3 (a launch buff was tried but overshot — diagnosed it broke the steep-V reverse test by carrying the bike to the far wall, so reverted). `SIM_VERSION` 12→13. Verified: 15/15 physics tests (incl. determinism + steep-V escape), verify clean (4 ws), web build clean. Old test runs (sim 12) cleared next.

- **P8.7 — favicon** (2026-06-19): wired favicon.ico (+16/32 png) + apple-touch-icon links in index.html; icon files in apps/web/public/. Frontend only.
- **P8.4 — strip dev** (2026-06-19): removed all dev/test scaffolding for launch — the Playground sandbox (`src/playground/*`, `screens/playground.ts`, its route + nav links), the BIKE TUNE slider panel (`src/dev/bikeTunePanel.ts`, `B`/`?biketune=1`) incl. the dev HUD/SELF-TEST, the `J`/`?showbodies=1` collision overlay (render.ts), and the `DEV_FORCE_CLOSE` force-close/force-settle admin endpoints (+ net `forceCloseWindow`, admin CLOSE NOW button, `.env.example` var). `BIKE_TUNE` stays (baked tune read by drawBike). KEPT: chart browser, real ride + score/combo HUD, music controls, account UI; ADDED a fading new-player controls hint "W throttle · Space jump · A/D lean" on ride start. No SIM change. Verified: verify clean (4 ws), api + web builds clean.

- **P8.12 — trick rebalance** (2026-06-19): scoring change — `trickWeight` 0.15→1.0 (tricks ~6.7× more) + `speedExp` 1.5→1.25 (beating-par reward flatter); `baseFinish` 10000 unchanged. `SIM_VERSION` **13→14** (confirmed real prior value was 13; P8.10 was never applied). Exposed `rawTrickPoints` on SimSnapshot/FinalResult (passive). Run-complete card has a **DEV-ONLY readout** (raw·speed·trickBonus·final·sim 14 (was 13)) — remove before launch. Updated the magnitude/exponent-dependent assertions in scoring.test + the maxPlausiblescore threshold (runVerify.test) to the new constants. Verified: 15/15 physics + 102/102 api tests, verify clean (4 ws), api+web builds clean. **Balance note**: at trickWeight 1.0, an extreme trick farm can rival a clean par finish on raw score — but DNFs never rank (eligibleForRank requires finished), so leaderboard integrity holds; dial via the dev readout.

- **P8.13 — trickWeight 0.35** (2026-06-19): dialed `trickWeight` 1.0→0.35 (still >2× the original 0.15; speedExp 1.25 + baseFinish 10000 unchanged). `SIM_VERSION` left at **14** intentionally — still dialing, nothing saving yet; lock version + clear boards right before deploy. DEV run-complete readout kept for one more pass. Tests still pass unchanged (lower weight only strengthens the finish-beats-farm invariants). Verified: 15/15 physics + 102/102 api, verify clean, web build clean.

- **P8.14 — multiplicative final score** (2026-06-19): final = `round(speedScore × (1 + rawTrickPoints/trickAmplifyK))`, new `trickAmplifyK = 25000`. Tricks amplify the speed score instead of adding, so a slow/crashy run (low speedScore) can't out-farm a clean fast one. `updateScore` sets the multiplicative final on the finished frame; the live HUD score stays the trick tally during the run (so HUD + flip popups read correctly). DNF still = trickBonus (rawTrickPoints×0.35) and never ranks. DEV run-complete readout updated → speed · raw · ×multiplier · final. SIM_VERSION still **14** (dialing). Verified: 15/15 physics + 102/102 api (no test edits needed — invariants hold), verify clean, web build clean. **Star rating not yet recalibrated** (reported for the new scale; see Pre-deploy lock).

- **P8.15 — star recalibration** (2026-06-19): `STAR_FRACTIONS` → `[0.1,0.2,0.32,0.46,0.66]` of `maxScore` (50000) → 1★ 5k / 2★ 10k / 3★ 16k / 4★ 23k / 5★ 33k (5★ now achievable: fast+clean+tricky). Added a next-star target under the stars on the run-complete card ("Next ★ <score>", or gold "MAX ★" at 5). Frontend only, no SIM bump. Verified: verify clean, web build clean.

**In progress**
- **Pre-deploy lock**: when the score balance is final, do a clean SIM_VERSION bump (currently 14) + clear cr_runs/cr_payouts (boards), and remove the DEV run-complete readout (`rc-dev`, P8.12).

**Next**
- **API cleanup (later)**: drop the legacy `difficulty`/`tracks` fields from `/api/maps` (superseded by tiers; unread by the client).
- **Manual browser pass**: Home (6 clean coin cards) → MapDetail period tabs (1Y/6M/3M) + tier selector (VOLATILE/DEGEN/SAVAGE) + mode reach every track, SAVAGE magenta/glow → ride to finish, confirm auto-submit status.
- **P7 runtime checks (owner)**: unattended cron opens/settles windows on `:00/:30` for 2+ h; a two-account race on one paying track → exactly one pending payout at the higher score; admin phone flow (copy→Phantom→paste sig→green→appears on `#/payouts` solscan). Single API instance only (in-process cron).
- **ALL-period maps still blocked**: CoinGecko keyless+demo key both 401 on `days=max` (365-day cap is paid-only — confirmed). Decision taken: **add a Binance source** (free, full history via weekly klines) — not yet built. Needs `cr_maps` source check widened to include `binance` + a `chartdata.ts` Binance fetcher.
- Owner: apply the HARDENING section of `apps/api/sql/001_track_pipeline.sql` in the Supabase SQL editor (freeze trigger, unique indexes, period-constraint widening for 90D/180D memecoin maps).
- Memecoin maps: paste GeckoTerminal pool addresses into the two placeholders in `apps/api/scripts/seed-maps.ts`.
- Difficulty calibration: all three 1Y majors land "insane" (real daily candles at 6m spacing are steep) — thresholds or SPACING_M may need a pass once playable.
- (DONE in P5 — neon light-cycle + glow trail + skins shipped.)
- API: wire `simulateReplay` into run submission validation (P6) — `cr_runs` already exists in the live schema.

---

# Appendix A — Bike rig spec (from `docs/PHYSICS_RESEARCH.md`)

Units: meters, kg (via density × area), seconds, radians. Gravity `(0, -10)`. Step `world.step(1/60, 8, 3)`. **Starting values to tune.**

**Bodies / fixtures**

| Body | Type | Shape | Density | Friction | Notes |
|---|---|---|---|---|---|
| Chassis (frame+rider mass lumped) | dynamic | polygon ~1.4 m × 0.4 m | 1.0 | 0.3 | Heavy body; tune mass ~**8–10:1** vs wheels |
| Rear wheel | dynamic | circle **r=0.4** | 1.0 | **1.6** | Driven; high friction = bite |
| Front wheel | dynamic | circle **r=0.4** | 1.0 | **1.6** | Free-rolling |
| Head sensor | fixture on chassis | circle r≈0.18, **isSensor** | — | — | Death trigger |
| Ground | static | open **Chain** (chart polyline) | — | 0.6 | One body, many verts |

Set `bullet = true` on wheels + chassis (anti-tunnel). Rotation enabled.

**Joints (2 × WheelJoint, `localAxisA = (0,1)`)**
- **Rear:** `frequencyHz 5.0`, `dampingRatio 0.8`, `enableMotor true`, `motorSpeed = -throttle * MAX_OMEGA` (MAX_OMEGA ≈ 50 rad/s), `maxMotorTorque 20` (→40 for more punch).
- **Front:** `frequencyHz 5.0`, `dampingRatio 0.8`, `enableMotor false`, `maxMotorTorque 5` (brake only).

**Control model (per fixed step)**
- Throttle: rear `setMotorSpeed(-MAX_OMEGA)`, full torque.
- Brake: ramp motor speeds → 0 with high torque.
- Lean (X-Moto pattern, applied to chassis):
  ```
  attitude = lean * ATTITUDE_TORQUE   // ATTITUDE_TORQUE ≈ 1500–3000 N·m
  chassis.applyTorque(attitude)
  attitude *= 0.75                     // decay 25%/step
  if (|attitude| < threshold) attitude = 0
  ```
  Tune so a held lean ≈ one full rotation per medium jump.

**Crash / landing / death**
- Death: head sensor `begin-contact` with ground **+** swept circle check (last→current head pos) for fast face-plants.
- Hard landing: in `post-solve`, big normal impulse **and** chassis-vs-ground angle outside tolerance → crash. **Landing forgiveness:** ±~25–35° between chassis angle and local slope is OK (keep speed).
- Respawn: snap full state (pos, angle, lin+ang velocity, motor state) to last checkpoint.

**Game loop:** fixed-timestep accumulator, `MAX_FRAME = 0.25` clamp, input sampled once per fixed step, render interpolates with `alpha = accumulator / STEP`. Server runs the same loop minus rendering over the recorded input log.

**Determinism essentials:** pin Planck version everywhere; fixed dt + fixed iters; input recorded as `(stepIndex, action)`; seeded/no RNG in sim; no wall-clock in sim; deterministic terrain build (pin data snapshot); stable body/joint creation order; exact snapshot/restore; CI parity test (browser vs Node → identical score + transform hash).

---

# Appendix B — Neon bike visual spec (from `docs/ART_RESEARCH.md`)

**Pipeline:** code-drawn **Canvas2D vector bike** (primary). AI sprites only for off-physics hero/marketing art. Asset packs only as disposable placeholder. Look = **neon motocross/dirt bike** (gameplay + IP differentiator from Tron's road cycle).

**Segments (Canvas2D paths):** frame spine, seat/tank wedge, front fork, rear swingarm, front wheel (ring+spokes+hub on front body), rear wheel (on rear body), minimal angular rider leaning with input. Wheels use their own physics body transforms; everything else on the chassis transform.

**Glow recipe (per emissive stroke, additive `globalCompositeOperation="lighter"`):**
1. Outer halo: `shadowBlur 18`, stroke `rgba(accent,0.25)` width 6.
2. Mid glow: `shadowBlur 8`, stroke `rgba(accent,0.60)` width 4.
3. Core: `shadowBlur 0`, stroke `#ffffff` width 2.
Round caps/joins. Reset to `source-over` after. **White core is never skinnable** (legibility). Cap blurred passes / offer "lite glow" on low-end.

**Rear light ribbon:** ring buffer of last **40 rear-wheel positions** (push once per fixed sim step → deterministic). Polyline newest→oldest, width `lerp(full,0)` and alpha `lerp(0.7,0)`, color `trailHex`, additive + small blur. Clear on respawn.

**Skin config (`skins.json`, day one):** `{ id, name, primary, secondary, trail, unlock }`. `unlock.type` ∈ `default | score(min) | daily | achievement(id) | token(chain,contract,minBalance)`. Renderer reads only `primary`/`secondary`/`trail`; new skin = append one object. Skin logic stays out of the deterministic sim (cosmetic only).

**Draw order at rest (back → front):** 1) rear light ribbon, 2) rear wheel, 3) front wheel, 4) rear swingarm, 5) front fork, 6) frame spine, 7) seat/tank wedge, 8) rider. Within each emissive element: outer halo → mid glow → white core.
