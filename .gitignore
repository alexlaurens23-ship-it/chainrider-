# dependencies
node_modules/

# build output
dist/

# secrets / env — ignore ALL env files, keep only the committed templates
.env*
!.env.example
!.env.sample

# launch tooling output
launch-result.json

# misc
.DS_Store
*.log
