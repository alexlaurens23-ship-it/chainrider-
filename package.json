{
  "name": "chainrider",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/physics",
    "apps/web",
    "apps/api",
    "scripts/launch"
  ],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "npm run build -w @chainrider/physics && npm run build -w @chainrider/web && npm run build -w @chainrider/api",
    "verify": "npm run build -w @chainrider/physics && npm run verify --workspaces --if-present",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
