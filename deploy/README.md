# deploy/

Deployment configuration for CHAINRIDER (hosting configs, Dockerfiles, infra scripts).

Empty for now — populated when the api/web apps get a deployment target. Remember the hard rules: no secrets committed here, `.env` files only.
